export { default, size, contentType, alt } from './opengraph-image'

/** Declared, not re-exported — route-segment config is parsed statically from this file. */
export const dynamic = 'force-static'
